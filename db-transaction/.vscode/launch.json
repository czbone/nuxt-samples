{
    "version": "1.0.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "プログラムの起動",
            "program": "${workspaceFolder}/main.js",
            "cwd": "${workspaceFolder}",
            "outputCapture": "std"
        }
    ]
}